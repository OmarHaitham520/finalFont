import React from "react";
import {
  View,
  Text,
  TextInput,
  Button,
  StyleSheet,
  Image,
  TouchableOpacity,
  ActivityIndicator,
} from "react-native";

const cart = (props) => {
  return;
  <View>

      
  </View>;
};

const styles = StyleSheet.create({
  new:{

  }
});
export default cart;
